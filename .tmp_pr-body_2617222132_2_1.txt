### Update dependencies

Automatically created PR from [`ci/dependency-updates`](https://github.com/CasperWA/gh-actions/tree/ci/dependency-updates).

The PR is based on the ['CI - Update dependencies PR' workflow](https://github.com/CasperWA/gh-actions#ci---update-dependencies-pr-ci_update_dependenciesyml) in [CasperWA/gh-actions](https://github.com/CasperWA/gh-actions).

#### To do

- [ ] Check that the diff is sensible, and that tests and builds pass with the new dependency versions.
- [ ] Make sure the PR is **squash** merged, with a sensible commit message.
